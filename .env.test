# Environment variables for Vitest / integration tests
# Safe, test-only values

JWT_SECRET=test_jwt_secret_key
JWT_EXPIRES_IN=1h

# Not used in tests because we use in-memory Mongo, but defined for completeness
MONGO_URI=mongodb://127.0.0.1:27017/erinde_test
PORT=4000
